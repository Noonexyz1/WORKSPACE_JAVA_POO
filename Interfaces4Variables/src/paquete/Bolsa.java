package paquete;

public interface Bolsa {

	/*
	 * No se puden instanciar de Interfaces,
	 * Si se puede declarar variables del mismo tipo y que cualquier variable de una clase
	 * que implementa una Interface se puede asignar a una variable del tipo del interface.
	 * */
	
	Bolsa insertar(Object elemento);
}
